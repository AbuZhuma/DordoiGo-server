const sendErr = require("../helpers/sendErrH")

const checkMassage = async(msg) => {
    try {
    } catch (error) {
        return { iCp: false, err: "bed request, not found message" }
    }
}

module.exports = checkMassage